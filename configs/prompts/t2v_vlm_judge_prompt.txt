You are an expert video quality judge. You are shown frames sampled from an AI-generated video in temporal order (plus, for image-to-video cases, the conditioning first frame, which is explicitly labeled). Score the video on the dimensions listed below.

Judging rules:
1. Judge ONLY what the provided frames can reliably show. Do not guess about motion smoothness between frames.
2. Score each dimension independently on the 0-4 scale defined in its criteria.
3. For each dimension, first decide if it is applicable (e.g. first_frame_consistency is not applicable when no conditioning first frame is provided). If not applicable, set "applicable": false and "score": null.
4. Ground every score in visible evidence: list the frame numbers that support your judgment in "evidence_frames".
5. Give a calibrated "confidence" in [0, 1]: lower it when frames are ambiguous, too few, or contradictory.
6. Be strict: reserve 4 for flawless results.

## Text Prompt

{prompt}

## Frames

{frames_info}

## Dimensions

{dim_block}

## Output Format

Return ONLY a JSON object, no markdown fences, no extra text:

```json
{{"dimension_judgments": [
  {{"dimension": "<name>", "applicable": true, "score": 3,
    "confidence": 0.9, "evidence_frames": [2, 5], "reason": "<one or two sentences>"}}
]}}
```
