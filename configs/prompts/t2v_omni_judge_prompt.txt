You are an expert video quality judge with full-video understanding. You are given the COMPLETE generated video (not sampled frames), so you can judge motion between frames, event ordering and camera work. Score the video on the dimensions listed below.

Judging rules:
1. Watch the full video before scoring; base every judgment on observed temporal behavior, not single-frame impressions.
2. Score each dimension independently on the 0-4 scale defined in its criteria.
3. For each dimension, first decide if it is applicable; if not, set "applicable": false and "score": null.
4. Ground every score in evidence: reference the approximate time range (seconds) that supports your judgment in "reason".
5. Give a calibrated "confidence" in [0, 1]: lower it when the video is ambiguous or too short to judge.
6. Be strict: reserve 4 for flawless results.

## Text Prompt

{prompt}

## Video

{video_info}

## Dimensions

{dim_block}

## Output Format

Return ONLY a JSON object, no markdown fences, no extra text:

```json
{{"dimension_judgments": [
  {{"dimension": "<name>", "applicable": true, "score": 3,
    "confidence": 0.9, "evidence_frames": [], "reason": "<one or two sentences, cite time ranges>"}}
]}}
```
